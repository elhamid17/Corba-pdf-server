<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- ═══ Coordonnées du projet parent ═══ -->
    <groupId>sn.ussein</groupId>
    <artifactId>corba-pdf-server</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>CORBA PDF Server</name>
    <description>
        Serveur distribué de traitement PDF — JacORB, PDFBox, Spring Boot, React
    </description>

    <!-- ═══ Modules ═══ -->
    <modules>
        <module>corba-idl</module>
        <module>corba-server</module>
        <module>api-gateway</module>
    </modules>

    <!-- ═══ Propriétés globales ═══ -->
    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Versions des dépendances — on les gère ici, une seule fois -->
        <jacorb.version>3.9</jacorb.version>
        <pdfbox.version>2.0.31</pdfbox.version>
        <tess4j.version>5.8.0</tess4j.version>
        <bouncycastle.version>1.78.1</bouncycastle.version>
        <poi.version>5.2.5</poi.version>
        <springboot.version>3.2.5</springboot.version>
        <junit.version>5.10.2</junit.version>
    </properties>

    <!-- ═══ Gestion centralisée des dépendances ═══ -->
    <dependencyManagement>
        <dependencies>

            <!-- JacORB — ORB CORBA Java -->
            <dependency>
                <groupId>org.jacorb</groupId>
                <artifactId>jacorb</artifactId>
                <version>${jacorb.version}</version>
            </dependency>
            <dependency>
                <groupId>org.jacorb</groupId>
                <artifactId>jacorb-omgapi</artifactId>
                <version>${jacorb.version}</version>
            </dependency>

            <!-- Apache PDFBox — traitement PDF -->
            <dependency>
                <groupId>org.apache.pdfbox</groupId>
                <artifactId>pdfbox</artifactId>
                <version>${pdfbox.version}</version>
            </dependency>

            <!-- Tess4J — OCR Tesseract -->
            <dependency>
                <groupId>net.sourceforge.tess4j</groupId>
                <artifactId>tess4j</artifactId>
                <version>${tess4j.version}</version>
            </dependency>

            <!-- BouncyCastle — signatures PDF PKI -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcpkix-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>

            <!-- Apache POI — Word (DOCX) & Excel (XLSX) -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi</artifactId>
                <version>${poi.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${springboot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- JUnit 5 -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- ═══ Plugins Maven globaux ═══ -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>17</source>
                        <target>17</target>
                        <encoding>UTF-8</encoding>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <!-- ═══ Dépôt JacORB ═══ -->
    <repositories>
        <repository>
            <id>jacorb</id>
            <name>JacORB Repository</name>
            <url>https://www.jacorb.org/maven</url>
        </repository>
    </repositories>

</project>